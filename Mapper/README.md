To run this mapper, you will need to install Leaflet.heat at: https://github.com/Leaflet/Leaflet.heat
Note: download and unzip the files from the github, then run the command listed to install.

To run the example, simply place the included html file in /demo of the leaflet installation. Then open the html file in a browser.
